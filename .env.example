# .env
# VRAM der verfügbaren GPU in GB (gültige Werte z.B. 8, 12, 16, 24)
# Dieser Wert steuert, welche Modelle im Ollama-Container geladen werden.
GPU_VRAM=12

# --- NEU: Email Connector Konfiguration ---
# Welcher Provider soll genutzt werden? (z.B. "imap", später "google", "microsoft")
MAIL_PROVIDER=imap

# IMAP Zugangsdaten
MAIL_IMAP_HOST=imap.gmx.net  # Beispiel für GMX
MAIL_USER=ihre-email@gmx.de
MAIL_PASSWORD=ihr-app-passwort # Wichtig: Oft wird ein App-spezifisches Passwort benötigt!

# --- NEU: Datenbank Konfiguration ---
POSTGRES_DB=email_processor_db
POSTGRES_USER=admin
POSTGRES_PASSWORD=supersecretpassword